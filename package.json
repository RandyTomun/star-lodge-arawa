{
  "name": "star-lodge-arawa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp *.html *.css script.js dist/ && cp -R public dist/public"
  }
}
